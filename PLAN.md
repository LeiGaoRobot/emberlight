# Emberlight 三小时打磨计划(供 /goal 执行)

目标状态:从"可发布的 v1"到"值得转发的 v2"。每块做完必须过验证(见各块"验收"),
每完成两块就 `python build_dist.py` + 重新发布 Artifact(同 URL),失败回滚该块。
优先级从上到下,时间超了就砍最后两块,不砍验证。

验证手段固定:`__emberlight.simRun(120)` 分段跑整局看死亡时间/伤害来源;`step()` + 截图看画面;
`info()` 看 fps/draw call/三角面(基准:村庄 77 draw call、76 万面、8 分钟 240 敌人时 2 ms/帧)。

---

## 0. 开工 (5 min)
- 起 `emberlight`(8200)和 `emberlight-dist`(8202)两个服务器,确认 `?r=N` 强刷后无报错
- 读 README 的"难度曲线"记下当前基线:AI 打满 10 分钟,HP 最低 32

## 1. 铁匠铺升级武器 (25 min) ← 原推的核心卖点,现在只升属性
- 三把武器各 3 级,锻造币购买:
  - 灰烬弯月:弧度 +25% / 二段回斩(第二下反向)/ 命中回 1 HP
  - 余烬弹:穿透 +1 / 弹数 +1 / 命中爆炸小范围
  - 灰烬提灯:+1 盏 / 半径 +30% / 灼烧地面
- 铁匠铺弹窗加第二组"Weapons",武器卡片显示等级 ★;`FORGE` 价格表 12/24/40
- 天赋池里与武器重叠的(twin/bearer)改成加成而非重复
- 验收:模拟里 AI 在铁匠铺会买武器升级,`P.weaponRank` 生效且 DPS 数字变化;截图看弹窗

## 2. 敌人白天辨识度 + 打击感 (25 min)
- 敌人 body 桶加轮廓:InstancedMesh 背面放大 4% 的黑色描边副本(BackSide),一条 draw call
- 受击:实例矩阵加 0.08 s 缩放冲击 + 白闪已有;死亡改成 0.15 s 压扁再消失(用 `e.dying` 计时,不立刻删)
- 命中停顿:重击/新星命中 ≥3 只时 `timeScale` 0.05 持续 60 ms
- 爬行者扑击前 0.3 s 身体后缩 + 眼睛变亮(预警)
- 验收:白天村庄截图能一眼数出敌人;`step()` 死亡帧截图有压扁;帧率不掉

## 3. BOSS 战 (30 min)
- 登场演出:1.5 s 镜头拉近 BOSS + 横幅 + 地面裂纹环,期间玩家无敌
- 第二阶段(30% 血):体型 ×1.15、冲锋两连、砸地后留 3 处燃烧地面(复用 `S.burns`)
- 阶段切换横幅"THE WARDEN BURNS BRIGHTER";击杀后 3 s 慢镜 + 掉落雨
- BOSS 血条下加阶段小字;小地图 BOSS 图标脉动
- 验收:`spawnBoss()` 后 `step(3000)` 无报错,截图见二阶段;AI 模拟至少一次击杀 BOSS(可 `cheat` 提伤害只验流程)

## 4. 局外成长:灰烬档案 (25 min)
- localStorage `emberlight.meta`:累计击杀/最长存活/BOSS 击杀数/解锁项
- 解锁:活过 5 分钟 → 开局多 1 个天赋选项;击杀 BOSS → 开局送 10 锻造币;累计 3000 击杀 → 新起手武器"余烬弹"
- 标题页"Archive"面板:成就格子 + 历史前 5 局;结算页显示本局新解锁
- 验收:清 localStorage 后跑一局,结算页出现解锁提示,刷新后标题页保留

## 5. 设置与可达性 (20 min)
- 暂停菜单加 Settings:画质(High/Low:Low 关阴影、pixelRatio 1.0、草丛实例减半)、屏幕抖动开关、伤害数字开关、音乐/音效两个滑杆
- 中文界面开关:所有 UI 文案抽成 `STR` 表,`zh`/`en` 两套(横幅、天赋、铁匠铺、结算都要)
- 验收:切 Low 后 `info().tris` 明显下降且无报错;切中文后截图全页无英文残留

## 6. 音乐分层 (20 min)
- 现有 pad 上叠两层:威胁 ≥ 5 加低音脉冲,BOSS 期间加高频琶音;层的增益随 `setTension` 平滑
- 新增:区域切换时一小段音阶提示;胜利/失败各自动机
- 验收:`AUDIO` 无异常;手动 `setTension(1)` 时 analyser 能量上升(用 `AnalyserNode` 读一次)

## 7. 弱机性能档 (15 min)
- 距玩家 > 45 单位的草丛/芦苇实例不渲染(按区域预分桶,切换 `count`)
- 阴影相机随缩放收紧;`camDist` < 20 时阴影 4096,> 34 时 1024
- 验收:草丛分桶后村庄 draw call 不超过 +6,三角面下降 ≥ 20%

## 8. 收尾 (15 min)
- `build_dist.py` 重跑,8202 验两个单文件,Artifact 重新发布并写版本 label
- README:新增"v2 内容"与"局外成长"两节;CLAUDE.md 加一行结论
- 三张新截图(BOSS 二阶段、中文界面、档案面板)`SendUserFile` 给用户

---

### 砍单顺序(超时时)
7 → 6 → 5 的中文部分 → 4 的历史记录

### 已知风险
- 描边副本会让 `MATS.body` 的 instanceColor 闪白也作用到描边,需单独材质
- 中文字体:界面用系统 CJK 字体即可,别引 Google Fonts 中文(几 MB)
- 面板后台 rAF 不跑:所有验证走 `step()`,别等真实时间
