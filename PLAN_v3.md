# Emberlight v3 → 1.0 交付计划

目标:从"能玩、能发"的 v2.4 走到 **可以挂到 itch.io / GitHub Pages 让陌生人玩、不需要作者在旁边解释的 1.0**。
"交付"在这里的定义(Definition of Done)见文末,所有块都为它服务。工时按本机链路(bpy + three.js + autopilot 验证)估,共约 **15 小时,分 5 个 3 小时的 /goal 会话**。

优先级:**P0** 不做不能叫交付;**P1** 做了明显更像成品;**P2** 有余力再做。每块附验收方法,验收沿用现有手段:`__emberlight.step()/simRun()`、投影数值、`capture()` 截图、`info()` 性能数。

---

## 会话 A(3 h)· 可靠性与兼容 —— 全 P0

陌生人打开链接时会遇到的第一批问题,现在一个都没验过。

| # | 内容 | 验收 |
|---|---|---|
| A1 | **真机触屏**:摇杆/圆钮在 iPhone Safari 与 Android Chrome 上实测;iOS 的 WebAudio 解锁要挂在 touchend;横竖屏切换时 HUD 不重叠;`100vh` 地址栏问题用 `dvh` | 手机截图两张;摇杆推动后 `info().t` 前进且角色位移方向正确 |
| A2 | **WebGL 失败兜底**:`WebGL2` 不可用 / 上下文丢失(`webglcontextlost`)时显示"你的浏览器不支持"或"点此重载",不是黑屏 | 用 `WEBGL_lose_context` 扩展人工触发,看到提示 |
| A3 | **模块加载失败兜底**:jsDelivr 打不开(国内网络常见)时给出明确提示;`dist/index.html` 改成 **three 也内联**(不依赖任何 CDN,字体退回系统字体),Artifact 版保持 +esm | 断网状态本地打开 `dist/index.html` 能玩 |
| A4 | **存档版本化**:`emberlight.meta/settings` 加 `v` 字段,读到旧结构时迁移而不是崩;localStorage 写失败(隐私模式)静默降级 | 写入旧格式 JSON 后刷新不报错 |
| A5 | **弱机路径**:集显笔记本档(pixelRatio 1、无 AO、阴影 1024、草丛减半)自动判定:首 5 秒 fps < 40 直接切 Low 并提示;Low 档下模拟 240 敌人 draw call ≤ 90 | 用 `SET.quality='low'` + `simRun` 到 8 分钟,`info()` 记录 |
| A6 | **手柄**:Gamepad API,左摇杆移动、右摇杆瞄准、A 冲刺 / B 重击 / X 新星 / Y 换武器 / Start 暂停;HUD 按键提示随输入设备切换 | 用 `navigator.getGamepads` 桩注入一个假手柄,验证 `S.keys` 映射 |
| A7 | **错误上报到界面**:`window.onerror` 捕获后在角落显示一行"出错了,已记录",并把最近 20 条日志放进暂停菜单"复制诊断信息",方便玩家反馈 | 人工抛错看到提示 |

## 会话 B(3 h)· 内容闭环 —— P0/P1

现在"探索五个区域"只是横幅,区域没有目的;这是玩家 3 分钟后流失的主因。

| # | 内容 | 优先级 | 验收 |
|---|---|---|---|
| B1 | **神龛事件**:每区神龛按 F 激活 → 45 s 守护波(该区特色敌人 ×2 刷怪)→ 成功奖励区域专属天赋(荒野:鬼火不再成群 / 废墟:吐火者弹丸可被普攻打掉 / 余烬冢:燃烧地面对己无害 / 银泽岸:水边移速 +15%)+ 20 锻造币;小地图神龛图标未激活闪烁 | P0 | autopilot 加"去最近神龛"策略,四个神龛都能在 8 分钟前打完 |
| B2 | **区域精英**:每区一只 mini-boss(荒野狼王 / 废墟石像 / 余烬冢火蜥 / 银泽岸淤泥巨口),kit 里各建一个骨架(复用 Warden 的 Empty 结构),各 2 个招式;首次击杀记入档案 | P1 | 每只 `spawnEnemy` 后 `step(3000)` 无报错、招式各触发一次 |
| B3 | **武器扩到 5 把**:+ 灰烬长弓(蓄力穿透)、余烬链(前方短距连击);铁匠铺各 3 级;起手武器可在标题页选择(解锁后) | P1 | 各武器 `simRun(300)` 击杀数在弯月的 60–140% 之间 |
| B4 | **天赋扩到 24 个**并加 3 个"钥石"(每局限一:玻璃大炮 / 提灯守护 / 灰烬吸血),升级弹窗有稀有度标色 | P1 | 天赋池随机 200 次无重复越界、无 max 溢出 |
| B5 | **BOSS 二号**:12 分钟(无尽)出"银泽夫人"(水系,召唤水波推人、雾里隐身),避免无尽模式只有守卫循环 | P2 | 同 B2 |
| B6 | **难度档**:静谧(伤害 −30%、无二阶段)/ 标准 / 灰烬(刷怪 +40%、守卫 6 分钟登场);标题页选择,档案按难度分记 | P0 | autopilot 三档各跑一局:静谧必过、灰烬 6 分钟内死 |

## 会话 C(3 h)· 引导与手感 —— P0/P1

| # | 内容 | 优先级 | 验收 |
|---|---|---|---|
| C1 | **首局引导**:前 60 s 依次弹 4 张提示卡(移动 → 自动攻击在打 → 捡余烬升级 → 铁匠铺在这),每张完成对应动作即消失;档案里记"已引导"不再弹 | P0 | 清档后新局按顺序出现,做完动作后消失 |
| C2 | **死亡复盘**:结算页加"你死于 X 的 Y 次攻击""最后 10 秒血量曲线"(小 canvas),用现有 `S.dmgLog` | P1 | 死一局后页面有曲线 |
| C3 | **敌人程序化动画**:实例矩阵里加每类节拍(爬行者腿部用第二组实例 IM 摆动、蛮兽落地震屏、吐火者吐前鼓包),不增加骨架 | P1 | draw call ≤ +4,截图能看出腿动 |
| C4 | **主角提灯光影**:提灯点光随 `P_Lantern` 摆动;夜里敌人靠近提灯眼睛更亮;新星时全屏一次曝光冲击 | P2 | 夜景截图 |
| C5 | **手感数值二轮**:根据 C2 数据把吐火者弹速/伤害、蛮兽命中判定、冲刺无敌帧再调一轮;autopilot 三难度基线写进 README | P0 | 三档 simRun 结果表 |
| C6 | **色弱可辨**:敌人眼睛颜色 / 拾取物加形状差异开关(余烬菱形、锻造币三角、红心圆) | P1 | 设置项存在且截图可辨 |

## 会话 D(3 h)· 音画收口 —— P1/P2

| # | 内容 | 优先级 | 验收 |
|---|---|---|---|
| D1 | **配乐重写**:三段式(村庄安宁 / 荒野紧张 / 守卫)各 32 小节的和弦进行 + 主旋律,仍用 WebAudio 合成但换成 4 层(pad/低音/琶音/旋律),按威胁与区域切换 | P1 | `layerState()` 在三种状态返回不同层组合 |
| D2 | **音效替换**:12 个关键音效(挥砍、命中、拾取、升级、锻造、冲刺、新星、受伤、守卫吼、砸地、胜利、失败)重做,加随机音高 ±6% | P1 | 试听表 |
| D3 | **天气加料**:暴风闪电照亮全场一帧 + 树冠摇摆(shader 顶点位移随风)、雪天呼吸白雾粒子、雨天地面反光已有再加水洼 | P2 | 三种天气截图 |
| D4 | **标题页镜头**:开场从高空飞入村庄 4 s,再进入闲逛;About 里放三张截图 | P2 | 截图 |
| D5 | **水面**:岸线外的海用简单波浪 shader + 泡沫线,替换现在的纯色 | P2 | 银泽岸截图 |

## 会话 E(3 h)· 打包发行 —— 全 P0

| # | 内容 | 验收 |
|---|---|---|
| E1 | **代码拆分**:`main.js`(2000 行)拆成 player / enemies / boss / weather / ui / meta 六个模块;`build_dist.py` 按 import 顺序拼接;调试钩子只在 `?debug=1` 时挂 | 拆完 `simRun(600)` 结果与拆前一致;dist 体积 ≤ 3 MB |
| E2 | **版本与更新日志**:`VERSION` 常量显示在标题页角落;`CHANGELOG.md`;GitHub Release 附 `emberlight-1.0.zip`(dist + README) | Release 页面可见 |
| E3 | **itch.io 页面**:`butler push dist itch/emberlight:html5`,封面 630×500、6 张截图、一段 15 s GIF(用 `capture()` 连拍合成);页面文案中英双语 | itch 链接可玩 |
| E4 | **许可与署名**:MIT LICENSE;About 与 README 注明灵感来源(歸藏推文)、字体许可(Fraunces OFL、Source Sans OFL)、three.js MIT | 文件存在 |
| E5 | **英文 README**:面向陌生开发者的 README.en.md(怎么跑、怎么改 kit、契约表),中文 README 保留 | 文件存在 |
| E6 | **回归清单跑一遍**:见下"发布前清单",全部打勾才发 | 清单文件带日期签名 |

---

## 发布前清单(每次发版执行)

1. `python build_kit.py`(如动过资产)→ 检查 GLB 动画数 = 19(或新的数)
2. `simRun` 三难度各一局,死亡时间 / 击杀数 / 伤害来源写进 `CHANGELOG`
3. 方向投影验证:D → (+,0),W → (0,−),鼠标右侧刀光/弹道 → (+,0)
4. 四种天气 × 昼夜各一张截图目检(无黑屏、无穿帮、路灯亮)
5. 暂停时升级、铁匠铺里升级、死亡瞬间升级三种弹窗冲突各试一次
6. 清 localStorage 后跑一局:引导出现、解锁写入、刷新后保留
7. `dist/index.html` 断网可玩;`artifact.html` 本地 8202 可玩;GitHub Pages 200
8. 手机横屏一局到 2 分钟不崩、不掉到 20 fps 以下
9. Low 档 8 分钟 `info()`:draw call ≤ 90、三角面 ≤ 350k
10. 控制台零报错(注意面板会缓存旧报错,以新加载的 `?v=` 为准)

## Definition of Done(1.0)

- 陌生人从链接进来,**不看任何说明**能在 60 s 内知道怎么动、怎么打、目标是什么(C1)
- 三个难度档,标准档真人首局存活 4–7 分钟、第五局左右能通关(C5 基线 + 真人 3 位试玩记录)
- 五个区域各有一件事可做、一个奖励可拿(B1),BOSS 不止一种打法(B2 至少两只)
- 桌面(Chrome/Edge/Firefox/Safari)、手机(iOS/Android)、手柄三种输入都能完整打完一局(A1/A6)
- 断网单文件可玩、CDN 挂了有提示、WebGL 不支持有提示(A2/A3)
- 版本号、更新日志、许可、署名、itch 页面齐全(E2–E5)
- 发布前清单 10 条全绿,签名进 `CHANGELOG`

## 砍单顺序(总工时超 15 h 时)

D5 → D4 → D3 → B5 → C4 → B3 的第二把武器 → D1 改为只加旋律层 → C3 只做爬行者

## 起跑命令

```
/goal 按 D:\AI\emberlight\PLAN_v3.md 的会话 A 逐块执行,每块过验收再进下一块,做完 A 后重打包发布并推送
```

之后依次把"会话 A"换成 B、C、D、E。每个会话结束时 `CHANGELOG.md` 追加一节,GitHub 推送一次。
